package xadrez;

import java.util.Scanner;

public class Main {
	
	public static void main(String[] args) {
		Tabuleiro.iniciaJogo();
/*		Boolean chequeMate = false;
		Boolean branca = true;
		int colunaIni = 0, linhaIni = 0, colunaFim = 0, linhaFim = 0;
		int cor = 1;
		Scanner scanner = new Scanner(System.in);
		do {
			//definir de quem é a vez
			if(branca) {
				cor = 1;
			}else {
				cor = 2;
			}
			//Escanear qual a movimentação do jogador
			System.out.println("Insira a peça que deseja movimentar: ");
			String peca = scanner.next();
			String dadoPeca = peca.toLowerCase();
			switch(dadoPeca) {
			case "rei":
				
			break;
			case "dama":
				
			break;
			case "bispo":
				System.out.println("Onde está seu bispo? (coluna/linha)");
				colunaIni = scanner.nextInt(); 
				linhaIni = scanner.nextInt();
				System.out.println("Para onde deseja movimentar? (coluna/linha)");
				colunaFim = scanner.nextInt(); 
				linhaFim = scanner.nextInt();
			break;
			case "cavalo":
				System.out.println("Onde está seu cavalo? (coluna/linha)");
				colunaIni = scanner.nextInt(); 
				linhaIni = scanner.nextInt();
				System.out.println("Para onde deseja movimentar? (coluna/linha)");
				colunaFim = scanner.nextInt(); 
				linhaFim = scanner.nextInt();
			break;
			case "torre":
				System.out.println("Onde está sua torre? (coluna/linha)");
				colunaIni = scanner.nextInt(); 
				linhaIni = scanner.nextInt();
				System.out.println("Para onde deseja movimentar? (coluna/linha)");
				colunaFim = scanner.nextInt(); 
				linhaFim = scanner.nextInt();
			break;
			case "peao":
				System.out.println("Onde está seu peao? (coluna/linha)");
				colunaIni = scanner.nextInt(); 
				linhaIni = scanner.nextInt();
				System.out.println("Para onde deseja movimentar? (coluna/linha)");
				colunaFim = scanner.nextInt(); 
				linhaFim = scanner.nextInt();
			break;
			}
			
			
			branca = !branca;
		}while(!chequeMate);
		
		
		*/
	}
}
